# Change: add schema CLI scripts to examples

- Date: 2025-12-28 08:42 PM PT
- Author/Agent: Codex
- Scope: examples
- Type: chore
- Summary:
  - add `schema:get`, `schema:publish`, and `schema:validate` scripts that wrap `onyx-schema` in the examples workspace
  - standardize the schema file path to `./onyx.schema.json` for all commands
  - document the convenience scripts in the README schema CLI section
- Impact:
  - no runtime or API changes; convenience scripts only
- Follow-ups:
  - none
