# Change: Add `onyx-schema info`

- Date: 2026-01-08 04:37 PM PST
- Author/Agent: Codex
- Scope: cli, config
- Type: feat
- Summary:
  - add `onyx-schema info` to print resolved connection details (id, base URL, masked credentials, config file, connection status)
  - expose config source metadata via `resolveConfigWithSource` for the CLI

- Impact:
  CLI only; no SDK surface changes.

- Follow-ups:
  None.
