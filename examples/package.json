{
  "name": "@onyx.dev/examples",
  "private": true,
  "type": "module",
  "dependencies": {
    "@onyx.dev/onyx-database": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "tsx": "^4.21.0"
  },
  "scripts": {
    "gen:onyx": "onyx gen --ts",
    "schema:get": "onyx schema get",
    "schema:publish": "onyx schema publish",
    "schema:validate": "onyx schema validate"
  }
}
